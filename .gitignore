# Netbeans project folder except private area
nbproject/private

# a config.ini is a customized version of config.tpl
# use config.tpl to create your own config.ini before doing the first run
.gitignore
beelog/config.ini
beelog/epaper_old
/WebPage



# ignore objects and archives, anywhere in the tree.
*.[oa]
